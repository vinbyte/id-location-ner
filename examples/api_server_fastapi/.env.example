# Copy to `.env` (do not commit `.env`) and set absolute paths.

# --- Server ---
LOCATION_NER_API_HOST="0.0.0.0"
LOCATION_NER_API_PORT=8000

# --- Required ---
# Path to Kemendagri-style CSV (full Indonesia or subset)
LOCATION_NER_CSV="/abs/path/to/location_kemendagri_2025.csv"

# --- Optional ---
# HuggingFace model name
LOCATION_NER_HF_MODEL="cahya/bert-base-indonesian-NER"

# Fuzzy matching threshold (0..100)
LOCATION_NER_FUZZY_THRESHOLD=90

# NER span min score for gating (0..1)
LOCATION_NER_NER_FILTER_MIN_SCORE=0.5

# Optional: load a custom env file instead of `.env`
# LOCATION_NER_ENV_FILE="/abs/path/to/custom.env"
